-- enCRAFTify Server Boot Script
-- Loads new station spawner (replaces old TileStationPostprocess)
-- Scans for existing tile patterns and converts them to entities on boot

require "StationMapper"

print("[enCRAFTify] Server module loaded - Station spawner ready")
